@model HistoryMvc.Models.PlanetSearch

@{
    ViewBag.Title = "Search Form";
}

<br />
<div class="row">
    <div class="span4">
        @using (Html.BeginForm("", "", FormMethod.Get, new { id="search-form", @class="form-horizontal" }))
        {
            <div class="control-group">
                @Html.LabelFor(m => m.Id, new { @class="control-label" })
                <div class="controls">@Html.TextBoxFor(m => m.Id)</div>            
            </div>
        
            <div class="control-group">
                @Html.LabelFor(m => m.Name, new { @class="control-label" })
                <div class="controls">@Html.TextBoxFor(m => m.Name)</div>
            </div>
            <div class="control-group">
                <div class="controls">
                    <button class="btn">Search</button>
                </div>
            </div>
        }
    </div>
</div>